# ensemble-buy-side-research 0.3.0

See `SKILL.md` for the operating contract and command examples. Run `ensemble-research demo --output runs/demo` for an offline smoke test, then `pytest` for the full test suite.
